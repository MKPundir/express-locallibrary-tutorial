extends layout

block content
  h1= title
  p Welcome to #[em LocalLibrary], a very basic Express website.

  h1 Dynamic Content

  if error
    p Error getting dynamic content
  else
    p Library has following record counts:
    ul
      li #[strong Books:] !{data.book_count}
      li #[strong Author:] !{data.author_count}
      li #[strong Book instances:] !{data.book_instance_count}
      li #[strong Book instance available:] !{data.book_instance_available_count}
      li #[strong Genre:] !{data.genre_count}